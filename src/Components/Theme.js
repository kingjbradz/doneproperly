import { createTheme } from "@mui/material/styles";

export const materialTheme = createTheme({
    palette: {
        brand: {
            primary: "#FFFEFE",
            secondary: "#000001",
            blorange: "#AB4F46",
            neopink: "#E274E7",
            neoyellow: "#DCF45C",
            lineblue: "#3C67D1"
        }
    },
    typography: {
        // fontFamily: 'typeface-cooper-hewitt'
    }
})