import { createTheme } from "@mui/material/styles";

export const materialTheme = createTheme({
    palette: {
        brand: {
            primary: "black",
            secondary: "#003153"
        }
    }
})