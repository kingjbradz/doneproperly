import React from "react"
import { Toolbar, Drawer, List, ListItem, ListItemText, ListItemIcon, IconButton } from "@mui/material"

const Sidebar = () => {
    return (
        <p>sidebar</p>
    )
}

export default Sidebar